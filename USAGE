Description:
    hashira-rails generates Rails applications, preconfigured with sensible
    defaults.

Example usage:
    # Instead of saying:
    rails new weblog
    # say:
    hashira-rails weblog
